import React from 'react';
import { withUserAuthenticator } from './with_user_authenticator';

describe('withUserAuthenticator  test', () => {
  it('withUserAuthenticator is defined', () => {
    expect(withUserAuthenticator).toBeDefined();
  });
});
